module.exports = (message, snap) => async function perm(permissja) {

    /*  let m = ''
    switch(permissja){
        case 'ADMINISTRATOR':
            if(message.member.permissions.has("ADMINISTRATOR")){
                return 
            } else {
                return snap({msg: 'Ta komenda wymaga uprawnień administratora'})
            }


            if(!message.member.permissions.has("ADMINISTRATOR")) m = 'Ta komenda wymaga uprawnień administratora'
            break
        case 'owner.guild':
            if(message.member.id != message.guild.owner.id) m = 'Ta komenda jest przeznaczona dla właściciela serwer!'
            break
        case 'owner.bot':
            if(message.member.id != '') m = 'Ojj, przykro mi, ta komenda jest przeznaczona tylko i wyłącznie dla właścicela bota! **Mateusz**`#4711`'
            break
        default: if(!message.member.permissions.has(`${permissja}`)) m = `Nie masz uprawnień! Ta komenda wymaga 
        permissji: \`${permissja}\``
            break
    }

    if(!permissja) return console.warn('Blad constructora! nie podano permissji \n Usage: ob.perm("Permissja")')
    if(m = ''){
        return 0
    } else {    
        return snap({
            msg: m
        })
    }*/
}